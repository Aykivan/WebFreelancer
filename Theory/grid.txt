fit-content() - ограничивает колонку до значени€ в скобках
0.1fr если прописыввать значени€ fr через 0., то элементы не будут автоматически раст€гиватьс€ во всю ширину

ќптимальна€ запись:
grid-template-columns: repeat(3, 1fr); - 3 колонки по 1fr

»мена областей:
grid-template-areas: "header header" - шапка в 2 колонки 
		     "side content" - сайд-бар + контент
“еперь необходимо присвоить данные имена с помощью следующего свойств:
grid-area: header/side/content; которое прописываем в классе необходимого блока

ќбща€ запись:
grid-template: значение дл€ р€дов / значение дл€ колонок;

grid-template:
		[start] "header header" 100px [row2] 
		[row2] "side content" 1fr [row-end] / 150px 1fr;
— помощью записи выше мы задаем первую строку и ее высоту
потом 2рую строку и ее высоту / ширина первой колонки ширина второй колонки

≈сли у нас определено 2 р€да по 3 колонки - они будут €вл€тс€ частью €вной сетки
≈сли при этом по€вл€етс€ 3 р€д с 7 элементом он будет €вл€тс€ не €вным
» дл€ управлением его параметров примен€етс€ свойство: 
grid-auto-rows: его значение;
grid-auto-columns - мы можем применить только тогда, когда у нас нет заданной €вной сетки, то есть свойства - 
	grid-template

ƒл€ автоматического заполнени€ пространства примен€етс€ свойство:
grid-auto-flow: row/columns/dense(заполн€ет все с изменением последовательности)